<!--pages/admin/index/index.wxml-->
<view class='container'>
    <view>
        <button id='{{index}}' class='index_btn' wx:for='{{btn_arr}}' wx:key="index" bindtap='web_jump'>{{item.right_name}}</button>
    </view>
</view>
